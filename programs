# !pip install transformers
from transformers import pipeline



model_name = "nlptown/bert-base-multilingual-uncased-sentiment"


from transformers import AutoTokenizer,AutoModelForSequenceClassification

model = AutoModelForSequenceClassification.from_pretrained(model_name)
tokenizer = AutoTokenizer.from_pretrained(model_name)

classifier = pipeline("sentiment-analysis",
                      model=model,
                      tokenizer=tokenizer)

classifier('ఈ రెస్టారెంట్ చాలా బాగుంది')
classifier("இந்த உணவகம் மிகவும் நன்றாக இருக்கிறது")
classifier('यह रेस्टोरेंट बहुत अच्छा है')
#########################################################################################
from transformers import pipeline

# Configure and load the model only once
qa_pipeline = pipeline(
    task="question-answering",
    model="distilbert-base-cased-distilled-squad"
)

# Context can be reused for multiple questions
context = """


"""

# Question 1
result1 = qa_pipeline(
    question="      ",
    context=context
)
print(result1["answer"])

# Question 2
result2 = qa_pipeline(
    question="    ",
    context=context
)
print(result2["answer"])

# Question 3
result3 = qa_pipeline(
    question="    ",
    context=context
)
print(result3["answer"])
